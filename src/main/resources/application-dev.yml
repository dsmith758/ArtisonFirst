spring:
  datasource:
    platform: mysql
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/artisonfirst
    username: root
    password: root
    testWhileIdle: true
    validationQuery: "/* ping */ SELECT 1"
  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 8080
  
  